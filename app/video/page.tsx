"use client";
import {useState} from "react";
export default function Video(){const[p,setP]=useState("A cute strawberry character discovers a secret fruit world, cinematic, funny, highly detailed, TikTok style");const[v,setV]=useState("");const[e,setE]=useState("");const[g,setG]=useState(false);async function go(){setG(true);setE("");setV("");try{const r=await fetch("/api/generate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({prompt:p,aspectRatio:"9:16"})});const d=await r.json();if(!r.ok)throw Error(d.error);let s=d.status;while(s!=="succeeded"&&s!=="failed"&&s!=="canceled"){await new Promise(x=>setTimeout(x,2500));const q=await fetch(`/api/status?id=${d.id}`);const z=await q.json();s=z.status;if(s==="succeeded")setV(z.output);if(s==="failed"||s==="canceled")throw Error(z.error||"Generation failed")}}catch(x){setE(x instanceof Error?x.message:"Generation failed")}setG(false)}return <main style={{minHeight:"100vh",background:"#09090b",color:"white",padding:40,fontFamily:"Arial"}}><div style={{maxWidth:900,margin:"auto"}}><h1>AI Video Maker</h1><p>Real AI video generation · 9:16 TikTok</p><textarea value={p} onChange={x=>setP(x.target.value)} style={{width:"100%",height:130,padding:16,borderRadius:12,background:"#18181b",color:"white",border:"1px solid #333"}}/><button onClick={go} disabled={g} style={{marginTop:16,padding:"14px 24px",borderRadius:10,fontWeight:700}}>{g?"Generating AI video…":"Generate video ✦"}</button>{e&&<p style={{color:"#ff7777"}}>{e}</p>}{v&&<div style={{marginTop:30}}><video src={v} controls autoPlay loop playsInline style={{maxWidth:420,width:"100%",borderRadius:18}}/><br/><a href={v} target="_blank" rel="noreferrer" style={{color:"white"}}>Open/download video ↗</a></div>}</div></main>}
